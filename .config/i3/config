# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#
# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!


# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.

font pango:Hack 10

set $mod Mod4

floating_modifier $mod

# No borders (small borders)
# new_window none
new_window pixel 1
new_float pixel 1

# GAPS
gaps inner 0
gaps outer 0

bindcode $mod+112                gaps inner current minus  5
bindcode $mod+117                gaps outer current minus  5
bindcode $mod+Shift+112          gaps inner current plus  5
bindcode $mod+Shift+117          gaps outer current plus 5

# start a terminal
bindsym $mod+Return exec urxvt

# kill focused window (q)
bindcode $mod+24 kill
bindcode $sup+24 kill

# start dmenu (a program launcher) (d)
bindcode $mod+40 exec dmenu_run

# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop


# change focus
# (j)
bindcode $mod+44 focus left 

# (k) 
bindcode $mod+45 focus down 

# (l)
bindcode $mod+46 focus up   

# (;) 
bindcode $mod+47 focus right 

# alternatively, you can use the cursor keys:

bindsym $mod+Left  focus left
bindsym $mod+Down  focus down
bindsym $mod+Up    focus up
bindsym $mod+Right focus right

# move focused window

# (j)
bindcode $mod+Shift+44 move left  

# (k)
bindcode $mod+Shift+45 move down 

# (l)
bindcode $mod+Shift+46 move up    

# (;)
bindcode $mod+Shift+47 move right  

# alternatively, you can use the cursor keys:

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation (h)
bindcode $mod+43 split h 

# split in vertical orientation (v)
bindcode $mod+55 split v

# enter fullscreen mode for the focused container
bindsym F11 fullscreen toggle

# change container layout (stacked, tabbed, toggle split)

# (s)
bindcode $mod+39 layout stacking     

# (w)
bindcode $mod+25 layout tabbed       

# (e)
bindcode $mod+26 layout toggle split 

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
# (a)
bindcode $mod+Shift+38 focus parent 

# focus the child container
# (d)
bindcode $mod+Shift+40 focus child 

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"


# switch to workspace
bindcode $mod+10 workspace $ws1
bindcode $mod+11 workspace $ws2
bindcode $mod+12 workspace $ws3
bindcode $mod+13 workspace $ws4
bindcode $mod+14 workspace $ws5
bindcode $mod+15 workspace $ws6
bindcode $mod+16 workspace $ws7
bindcode $mod+17 workspace $ws8
bindcode $mod+18 workspace $ws9
bindcode $mod+19 workspace $ws10

# move focused container to workspace
bindcode $mod+Shift+10 move container to workspace $ws1
bindcode $mod+Shift+11 move container to workspace $ws2
bindcode $mod+Shift+12 move container to workspace $ws3
bindcode $mod+Shift+13 move container to workspace $ws4
bindcode $mod+Shift+14 move container to workspace $ws5
bindcode $mod+Shift+15 move container to workspace $ws6
bindcode $mod+Shift+16 move container to workspace $ws7
bindcode $mod+Shift+17 move container to workspace $ws8
bindcode $mod+Shift+18 move container to workspace $ws9
bindcode $mod+Shift+19 move container to workspace $ws10

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
# (r)
bindcode $mod+Shift+27 restart 

# exit i3 
#bindcode $mod+Shift+26 exec i3-msg exit 

# resize window 
# (u)
bindcode $mod+30 resize shrink width 10px or 10ppt  
# (i)
bindcode $mod+31 resize grow height 10px or 10ppt   
# (o)
bindcode $mod+32 resize shrink height 10px or 10ppt 
# (p)
bindcode $mod+33 resize grow width 10px or 10 ppt   

mode "resize" {
   
    # These bindings trigger as soon as you enter the resize mode
	# Pressing left will shink the window’s width.
	# Pressing right will grow the window’s width.
	# Pressing up will shrink the window’s height.
	# Pressing down will grow the window’s height.

	bindcode 44 resize shrink width 10 px or 10 ppt
	bindcode 45 resize grow height 10 px or 10 ppt
	bindcode 46 resize shrink height 10 px or 10 ppt
	bindcode 47 resize grow width 10 px or 10 ppt

	# same bindings, but for the arrow keys

	bindsym Left resize shrink width 10 px or 10 ppt
	bindsym Down resize grow height 10 px or 10 ppt
	bindsym Up resize shrink height 10 px or 10 ppt
	bindsym Right resize grow width 10 px or 10 ppt

	# back to normal: Enter or Escape
	bindsym Return mode "default"
	bindsym Escape mode "default"
}
# (r)
#bindcode $mod+27 mode "resize" 

# Pulse Audio controls

bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume 0 +5% #increase sound volume
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume 0 -5% #decrease sound volume
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute 0 toggle # mute sound
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute 1 toggle

# Exec alsamixer for sound and audio
bindsym $mod+XF86AudioMicMute exec urxvt -e alsamixer


# Sreen brightness controls

bindsym XF86MonBrightnessUp exec light -A 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec light -U 20 # decrease screen brightness

# Touchpad controls

bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls

bindsym XF86AudioPlay exec mocp -G  
bindsym XF86AudioStop exec mocp -x
bindsym XF86AudioNext exec mocp --next
bindsym XF86AudioPrev exec mocp --prev

bindsym Shift+XF86Forward   exec mocp -k +10
bindsym Shift+XF86Back      exec mocp -k -10

# Lock computer
# (.)
bindcode $mod+Shift+60 exec --no-startup-id setxkbmap best_layout dvorak-classic && bash ~/.config/i3/lock.sh 

# Keyboard layouts

bindsym Shift+F7 exec --no-startup-id setxkbmap us
bindsym Shift+F8 exec --no-startup-id setxkbmap best_layout dvorak-classic
bindsym Shift+F9 exec --no-startup-id setxkbmap best_layout serbian-latin
bindsym Shift+F10 exec --no-startup-id setxkbmap best_layout dvorak-cyrillic

# Execute xmodmap in terminal
#bindsym $mod+Shift+F8 exec setxkbmap us dvorak-classic && urxvt -e xmodmap ~/.config/Scripts/remaps
#bindsym $mod+Shift+F9 exec setxkbmap tw && urxvt -e ~/.config/Scripts/remaps  
#bindsym $mod+Shift+F10 exec setxkbmap bg && urxvt -e ~/.config/Scripts/remaps 

# Set keyboard layout
exec_always --no-startup-id xset r rate 200 40
exec_always --no-startup-id xset r 66
exec_always --no-startup-id setxkbmap best_layout dvorak-classic 
#exec_always --no-startup-id urxvt -e xmodmap ~/.config/Scripts/remaps
#exec_always --no-startup-id urxvt -e xmodmap ~/.config/Scripts/remaps

# Turn off pc speakers
exec_always --no-startup-id sudo rmmod pcspkr

#Start feh background
exec_always bash ~/.fehbg
#exec_always --no-startup-id feh --bg-max ~/Pictures/.wallpaper
#Start tlp advanced battery manager
exec_always --no-startup-id sudo tlp start

# Start compton (transparency)
# Currently using transset-df
exec_always --no-startup-id compton --no-fading-openclose

# Compton +/- 5
 bindcode $mod + 110 exec python ~/Code/system/compton_change_opacity.py 5
 bindcode $mod + 115 exec python ~/Code/system/compton_change_opacity.py -5 

# bindcode $mod + 110 exec transset-df --point
# bindcode $mod + 115 exec python ~/Code/system/compton_change_opacity.py -5 


# Make screenshot (PrtSc)
bindcode $mod+108 exec scrot '%Y-%m-%d-%H:%M:%S_$wx$h.png' -e 'mv $f ~/Pictures/Screenshots/'

# Chromioum
bindsym Shift+F1 exec chromium

# arandr (screen settings)
bindsym $mod+F7 exec arandr

# Thunar (File Manager) (t)
bindcode $mod + 28 exec urxvt -e ranger
# bindcode $mod + Shift + 28 exec thunar

# Temporarily inhibit lid-switch
bindcode $mod + 127 exec urxvt -hold -e systemd-inhibit --what=handle-lid-switch sleep 1d


######################## COLOR CONFIGURATION ################################


set_from_resource $bg-color            i3wm.color0 #000
set_from_resource $inactive-bg-color   i3wm.color9 #000000
set_from_resource $text-color          i3wm.color7 #f3f4f5 
set_from_resource $inactive-text-color i3wm.color8 #a3a4a5
set_from_resource $urgent-bg-color     i3wm.color6 #E53935
set_from_resource $indicator           i3wm.color15 #0f0 

# window colors
#                       border              background         text                 indicator
client.focused          $bg-color           $bg-color          $text-color          $indicator
client.unfocused        $bg-color           $inactive-bg-color $inactive-text-color $indicator
client.focused_inactive $bg-color           $inactive-bg-color $inactive-text-color $indicator
client.urgent           $urgent-bg-color    $urgent-bg-color   $text-color          $indicator

#toggle exec_always ~/.config/polybar/launch.sh

bar{
    status_command i3status
    position top
}
